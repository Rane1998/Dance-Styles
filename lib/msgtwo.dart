import 'package:flutter/material.dart';

class AboutWidgetTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      title: Text('User Created Succesfully!'),
    );
  }
}
